# app/models/__init__.py
# Keep this file minimal to avoid circular imports / duplicate registrations.
# Import specific models by full path (e.g. `from app.models.user import User`)
# where you actually need them.

__all__ = []  # do not auto-import model classes here
# This file is intentionally left empty to avoid circular imports.